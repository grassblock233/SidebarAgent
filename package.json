{
  "name": "sidebar-agent",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate-icons": "node scripts/generate-icons.mjs",
    "check": "node --check background.js && node --check sidepanel.js && node --check options.js && node --check shared/constants.js && node --check shared/providers.js && node --check shared/storage.js && node --check shared/openai-compatible-client.js && node --check shared/ui-state.js",
    "test": "node --test"
  }
}
