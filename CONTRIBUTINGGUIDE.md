# SidebarAgent 贡献指南

感谢你参与 SidebarAgent 的开发。提交改动前，请先阅读并遵守本指南，确保功能、视觉和安全行为保持一致。

## 开发流程

1. 从最新的 `master` 创建功能分支，避免直接在发布分支上开发。
2. 一个提交只处理一个明确主题，不混入无关重构、格式化或生成文件变更。
3. 修改前确认工作区状态，不覆盖其他贡献者尚未提交的改动。
4. 完成后运行 `npm run check`、`npm test` 和 `git diff --check`。
5. Pull Request 应说明用户问题、实现方案、验证结果，以及权限或存储结构是否发生变化。

## 品牌规范

- 产品名称固定为 `SidebarAgent`，代码包名使用 `sidebar-agent`。
- 品牌主题色固定为 `#86E3CE`；CSS 中统一写作 `#86e3ce`。
- 全局颜色、焦点样式、圆角、阴影和动画变量统一维护在 `shared/theme.css`。组件应优先引用变量，不要复制主题色硬编码。
- 主题色衍生值为：悬停色 `#6fd4bd`、浅色背景 `#eafaf6`、深色文字 `#174f44`、焦点光环 `rgba(134, 227, 206, .3)`。
- Logo 原始文件为 `assets/logo-source.png`。不得拉伸、变色、添加文字或更换背景。
- 扩展图标由 `scripts/generate-icons.py` 生成。更新 Logo 后运行 `npm run generate-icons`，不要手工编辑 `icon-16.png`、`icon-32.png`、`icon-48.png` 或 `icon-128.png`。
- `16px` 和 `32px` 图标允许使用生成脚本中的紧凑裁切，以保证浏览器工具栏中的辨识度；大尺寸图标应保留完整构图。

## 界面规范

- 侧边栏按最窄 `300px` 宽度设计，并检查常见窄窗口和低高度窗口。
- 顶栏和输入区保持稳定，中间对话区承担主要滚动；动态内容不得导致关键操作按钮移出可视区域。
- 交互控件使用语义化 HTML，并提供可理解的 `title`、`aria-label`、状态属性和键盘操作。
- 图标按钮优先使用项目现有 SVG 图标风格；不引入远程脚本、远程字体或运行时图标依赖。
- 动画必须遵守 `prefers-reduced-motion`，不得依靠动画本身传达唯一状态。
- 新增颜色前先确认现有主题变量是否能够表达该语义，避免形成不一致的局部配色。

## JavaScript 规范

- 使用原生 ES Modules、`const`/`let`、两个空格缩进和分号，保持与现有代码一致。
- 优先使用浏览器标准 API 和结构化数据处理，不用字符串拼接模拟 JSON、URL 或协议解析。
- 函数应保持单一职责；重复的状态转换、验证或协议处理应提取到 `shared/` 模块。
- 新增或修改代码必须带有有价值的注释。模块入口、复杂算法、异步状态流、权限边界、存储迁移和协议兼容处理必须说明设计原因或约束。
- 注释解释“为什么这样做”以及边界条件，不逐行复述显而易见的语句。修改实现后必须同步更新相关注释，禁止保留失效注释。
- 面向用户的错误信息使用中文，内部标识符和稳定错误码使用英文。
- 不记录 API Key、Authorization 请求头、完整 API 响应或用户网页正文。

## Chrome 扩展规范

- 遵守 Manifest V3，不引入远程可执行代码。
- 新权限必须与明确的用户操作对应，并在 Pull Request 和 README 中说明用途。
- 网站访问权限优先使用 `optional_host_permissions`，只在用户主动操作时请求最小域名范围。
- API Key 和模型配置存入 `chrome.storage.local`；选区和对话存入 `chrome.storage.session`。
- 修改设置结构时必须保留旧版本迁移路径，并为迁移与无效数据补充测试。
- 视口文字读取仅处理实际可见的 DOM 文本，不得悄悄扩展为截图、OCR 或整页采集。

## 测试要求

- 共享解析、存储、提供商或状态逻辑发生变化时，必须增加或更新单元测试。
- HTML/CSS 结构变化应更新对应结构测试，并检查 `300px` 宽侧边栏下的可用性。
- API 测试不得访问真实服务或包含真实密钥，应使用可控的模拟响应覆盖成功、失败、超时和取消场景。
- 提交前至少执行：

```bash
npm run check
npm test
git diff --check
```

## 文档与版本

- 用户可感知的功能、权限、配置流程或限制变化必须同步更新 README。
- 发布版本时同步更新 `manifest.json`、`package.json` 和版本测试，并补充更新日志。
- 文档只写已经实现并验证的行为，不把计划中的功能描述为现有能力。
